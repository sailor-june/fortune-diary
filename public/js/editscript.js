const buttons = document.getElementsByTagName('button')
const cards= document.getElementsByClassName('card')
const reading={}
function updateCards(){
    
}
buttons.array.forEach(element => {
    element.addEventListener('click', updateCards)
    
});